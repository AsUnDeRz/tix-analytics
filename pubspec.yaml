name: tix_analytics
description: A new flutter module project.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sentry: ^4.0.4
  device_info: ^0.4.2+4
  firebase_analytics: ^6.0.0
  facebook_app_events: ^0.8.2
  flutuate_mixpanel: ^1.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  module:
    androidX: true
    androidPackage: com.example.tix_analytics
    iosBundleIdentifier: com.example.tixAnalytics
