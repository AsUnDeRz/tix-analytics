name: tix_analytics
description: A new flutter module project.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sentry: ^4.0.4
  device_info: ^0.4.2+4
  firebase_analytics: ^6.0.0
  flutuate_mixpanel: ^1.1.7
  facebook_app_events:
    git:
      url : https://github.com/AsUnDeRz/flutter_facebook_app_events.git


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  module:
    androidX: true
    androidPackage: com.example.tix_analytics
    iosBundleIdentifier: com.example.tixAnalytics
